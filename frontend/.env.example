# Comma-separated Spring Boot peer base URLs (no trailing slash)
VITE_P2P_NODE_URLS=http://localhost:5010,http://localhost:5011,http://localhost:5012

# UI replica target for status badges (default 3)
# VITE_TARGET_REPLICAS=3
